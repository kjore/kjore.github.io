# 大数据实验报告（实验 1.1 与实验 1.2）

## 一、实验基本信息

- **课程名称**：大数据实验
- **实验名称**：实验一 大数据实验环境构建及 HDFS 应用实践
- **学生姓名**：XXX
- **学号**：XXXXXXXXXX
- **班级**：XXX
- **实验时间**：XXXX 年 XX 月 XX 日

***

# 实验 1.1 基于华为云构建大数据实验环境

## 1. 实验目的

1. 了解华为云基础操作流程；
2. 学会购买并配置华为云 ECS；
3. 掌握华为云服务器、OBS 桶及访问密钥的基本使用方法。

## 2. 实验内容

本实验在华为云平台上购买并配置 4 台 ECS 服务器，创建 OBS 桶及并行文件系统，并获取后续实验所需的 Endpoint 与 AK/SK，为后续 Hadoop 集群搭建和 HDFS 实验提供基础环境。

## 3. 实验过程

### 3.1 购买 ECS 服务器

首先登录华为云控制台，进入“创建资源”页面，选择“弹性云服务器 ECS”，开始创建服务器。在购买过程中，按照实验指导要求完成以下配置：

- 区域选择：**华北-北京四**；
- 计费模式：**按需计费**；
- 可用区：**可用区 2**；
- CPU 架构：**鲲鹏计算**；
- 实例规格：**2 vCPUs | 4 GB**；
- 操作系统：**CentOS 7.6**；
- 系统盘：**40 GB**；
- 购买数量：**4 台**；
![1773750380373](image/大数据实验一/1773750380373.png)
![1773750393686](image/大数据实验一/1773750393686.png)

- 网络选择：**vpc-default**；
- 安全组：**webserver**；
- 公网 IP：**现在购买**，带宽 **20M**；
- 服务器名称：**zjy-2023211236**。
- ![1773750487818](image/大数据实验一/1773750487818.png)
- ![1773750498174](image/大数据实验一/1773750498174.png)
其中，4 台 ECS 中 1 台作为主节点，另外 3 台作为从节点，后续用于搭建 Hadoop 集群。

### 3.2 创建 OBS 桶与并行文件系统

在控制台中进入“对象存储服务 OBS”，创建与 ECS 同一区域的存储桶。桶名称按照实验要求设置为“zjy-2023211236”。随后在该桶下创建并行文件系统，并记录系统生成的 Endpoint 信息，以便后续实验调用。

### 3.3 获取访问密钥 AK/SK

进入“我的凭证”页面，选择“访问密钥”，新建访问密钥并下载密钥文件。该文件中包含 Access Key（AK）和 Secret Key（SK），后续访问 OBS 时需要使用。

### 3.4 启动与关闭服务器

在 ECS 控制台中查看所创建的 4 台服务器，确认命名符合要求并成功分配公网 IP。随后对服务器执行开机与关机操作，熟悉云服务器的管理方式。

## 4. 实验结果

通过本实验，成功获得以下资源：

- 4 台以“zjy-2023211236”命名的 ECS 服务器；
![1773752463031](image/大数据实验一/1773752463031.png)
ECS 本质上就是云端虚拟机，后续所有 Hadoop、JDK、HDFS 等软件都需要部署在这些服务器上。如果没有先准备好多台服务器，就无法模拟分布式集群环境。
从截图中看到服务器已经成功创建并处于运行状态，说明云主机申请成功，网络和系统盘等基础配置也已生效。公网 IP 的存在说明我可以通过 SSH 从本地远程登录服务器，进行后续的集群部署操作。ECS 相当于大数据平台的“硬件基础”，虽然实验中使用的是云服务器，但本质上它们承担了和真实物理集群中各个节点相同的作用。
- 1 个以“zjy-2023211236”命名的 OBS 桶；
![1773750663762](image/大数据实验一/1773750663762.png)
OBS 是华为云提供的对象存储服务，这里的桶可以理解为存放数据文件的逻辑容器。创建该桶的目的，是为后续实验提供可扩展的云端存储支持。大数据实验环境不仅包括“计算资源”，还包括“存储资源”。在实际大数据系统中，计算和存储通常是配合使用的，单独只有服务器并不能完整构成实验环境。
从系统界面返回结果可以看出桶已经创建成功，这说明存储服务已经可以正常使用。桶名称、区域等信息的正确性也证明我的配置符合实验要求。通过这一过程，我进一步理解了云平台中计算、存储、网络三者协同工作的基本模式。
- 已保存到本地的 Endpoint 信息；
![1773750709356](image/大数据实验一/1773750709356.png)

- 已下载并保存到本地的 AK/SK 文件。
![1773750832012](image/大数据实验一/1773750832012.png)
AK/SK 可以理解为调用云服务接口时的身份标识，相当于用户名和密码的组合，但它更多用于系统之间的接口访问，而不是人工登录控制台。

## 5. 实验分析

通过实验 1.1，我完成了华为云基础资源的申请与配置，掌握了 ECS 的创建流程、OBS 桶的建立方法以及访问密钥的获取方式。该实验为后续 Hadoop 集群搭建提供了必要的硬件环境和对象存储支持。

在实验过程中，我进一步理解了云计算平台中“计算资源 + 存储资源 + 访问认证”的基本结构。ECS 提供计算能力，OBS 提供分布式对象存储，而 AK/SK 则用于身份认证和接口调用。通过实际操作，我对华为云控制台的使用方式有了初步认识，也认识到合理关闭服务器能够有效节约实验成本。

## 6. 实验小结

实验 1.1 的完成说明大数据实验环境已经初步搭建成功。后续可以在此基础上进一步部署 Hadoop 集群，并进行 HDFS 文件操作和 MapReduce 实践。

---

# 实验 1.2 安装 Hadoop 及 HDFS 应用实践

## 1. 实验目的

1. 学习 Hadoop 集群的搭建流程；
2. 学习 Maven 工程的创建与依赖管理；
3. 掌握 HDFS 文件系统的基本读写操作。

## 2. 实验内容

本实验基于实验 1.1 中购买的 4 台华为云 ECS，搭建 Hadoop 集群环境，完成 JDK 和 Hadoop 的安装配置，并通过 IDEA 创建 Maven 工程，编写程序实现对 HDFS 中文件的上传、读取等基本操作。

## 3. 实验过程

### 3.1 远程登录服务器

使用终端工具通过 SSH 命令远程连接到服务器。连接格式如下：

```bash
ssh root@公网IP
```

成功输入密码后，即可登录到对应节点进行后续配置。

### 3.2 配置免密登录

为了保证 Hadoop 集群中各节点之间能够相互通信，需要在主节点和所有从节点之间配置 SSH 免密登录。这样在启动 Hadoop 集群时，主节点就可以无需重复输入密码，直接远程调用其他节点上的服务。

本实验中共使用 4 台服务器，其中 1 台为主节点，3 台为从节点。假设主节点主机名为 node1，从节点分别为 node2、node3、node4。

#### 在各节点生成 SSH 密钥对

首先，在 node1、node2、node3 和 node4 四台服务器上分别执行以下命令生成 RSA 密钥对：
```bash
ssh-keygen -t rsa
```

执行后系统会提示输入密钥保存路径和密码，连续按回车即可，表示使用默认路径并且不设置密码。生成完成后，在每个节点的 ~/.ssh/ 目录下会出现以下文件：
```
id_rsa：私钥文件；
id_rsa.pub：公钥文件。
```

#### 查看并复制各节点公钥内容

在每个节点执行以下命令，查看本机公钥内容：
```bash
cat ~/.ssh/id_rsa.pub
```
将 node1、node2、node3、node4 四台机器输出的公钥内容分别复制保存下来。

（3）配置 authorized_keys 文件

为了实现节点之间免密访问，需要把所有节点的公钥都追加到每台服务器的 ~/.ssh/authorized_keys 文件中。也就是说，每个节点的 authorized_keys 文件中都要包含 4 台机器的公钥。

可以在每个节点执行如下命令：
```bash
vim ~/.ssh/authorized_keys
```
将 4 个节点的公钥内容逐行粘贴进去，每个公钥占一行，保存退出。


#### 配置主机名映射

为了便于节点之间通过主机名互相访问，还需要修改每台服务器的 /etc/hosts 文件，将各节点的私有 IP 地址与主机名进行映射。执行命令：
```bash
vim /etc/hosts
```
加入如下内容（以下 IP 仅为示例，需替换为你实际实验中的私网 IP）：

192.168.0.246 node4
192.168.0.8   node2
192.168.0.47   node1
192.168.0.130   node3

保存后，4 台机器都需要保持相同的 hosts 配置。
在本地机器的hosts文件中也需要添加这四台服务器的公网 IP 地址和主机名映射，以便在本地通过主机名访问各节点。通过管理员权限打开本地的 hosts 文件（Windows 系统路径为 C:\Windows\System32\drivers\etc\hosts），添加相同的映射关系，保存后即可。
![1773751831148](image/大数据实验一/1773751831148.png)

#### 测试节点间免密登录

配置完成后，在主节点 node1 上分别执行以下命令测试是否能够免密登录到其他节点：

ssh node2
ssh node3
ssh node4
![1773751423310](image/大数据实验一/1773751423310.png)
如果能够直接登录而不需要输入密码，则说明主节点到从节点的免密登录配置成功。

为了保证集群内部任意节点之间都可以通信，还可以进一步在其他节点上测试，例如：
若各节点之间均可直接访问，则说明 4 台服务器之间已经成功建立 SSH 免密登录关系。

#### 配置结果说明

完成以上配置后，集群中的各个节点就能够通过 SSH 实现无密码互访。该步骤是 Hadoop 分布式环境搭建的重要基础，因为 Hadoop 在启动和停止集群服务时，需要通过 SSH 自动连接其他节点并执行相关命令。如果没有正确配置免密登录，后续执行 start-dfs.sh、start-yarn.sh 等命令时将会出现连接失败或需要频繁输入密码的问题。

### 3.3 关闭防火墙

为避免 Hadoop 运行过程中出现端口访问问题，在每个节点执行以下命令关闭防火墙：

```bash
systemctl stop firewalld
systemctl disable firewalld
```

### 3.4 安装 JDK 1.8

在各节点通过 yum 安装 OpenJDK 1.8：

```bash
yum install java-1.8.0-openjdk.aarch64
```

配置环境变量：

```bash
echo "export JAVA_HOME=/usr/lib/jvm/java-openjdk" >> /etc/profile
source /etc/profile
java -version
```
![1773751475819](image/大数据实验一/1773751475819.png)
确认 Java 版本输出正常，说明 JDK 安装成功。

### 3.5 安装 Hadoop

在主节点下载 Hadoop 安装包：

```bash
wget https://mirrors.huaweicloud.com/apache/hadoop/common/hadoop-2.10.2
/hadoop-2.10.2.tar.gz
```

然后通过 `scp` 分发到其他节点：

```bash
scp -r hadoop-2.10.2.tar.gz node2:/root/
scp -r hadoop-2.10.2.tar.gz node3:/root/
scp -r hadoop-2.10.2.tar.gz node4:/root/
```

各节点解压并授权：

```bash
tar -xvf hadoop-2.10.2.tar.gz
chmod -R 777 hadoop-2.10.2
```

配置 `hadoop-env.sh`：

```bash
echo "export JAVA_HOME=/usr/lib/jvm/java-openjdk" >> ~/hadoop-2.10.2/etc
/hadoop/hadoop-env.sh
```

配置环境变量：

```bash
echo "export HADOOP_HOME=/root/hadoop-2.10.2" >> /etc
/profile
echo "export PATH=$HADOOP_HOME/bin:$HADOOP_HOME/sbin:$PATH" >>/etc/profile
source /etc/profile
```

### 3.6 配置 Hadoop 集群

根据实验指导，对 `core-site.xml`、`hdfs-site.xml`、`mapred-site.xml`、`yarn-site.xml` 和 `workers` 等配置文件进行修改，主要是配置先前得到的endpoint和AK/SK以及node1的私有IP地址，使 Hadoop 能够识别主节点和从节点，并指定 NameNode、DataNode、ResourceManager 和 NodeManager 等服务角色。

配置完成后，在主节点格式化 NameNode：

```bash
hdfs namenode -format
```

随后启动 HDFS 与 YARN：

```bash
start-dfs.sh
start-yarn.sh
```

使用 `jps` 查看各节点运行进程，确认 NameNode、DataNode、SecondaryNameNode、ResourceManager、NodeManager 等服务均已成功启动。
![1773751595995](image/大数据实验一/1773751595995.png)
![1773751621288](image/大数据实验一/1773751621288.png)
![1773751650366](image/大数据实验一/1773751650366.png)
![1773751663988](image/大数据实验一/1773751663988.png)
### 3.7 HDFS 文件操作实践

在 IDEA 中创建 Maven 工程，使用jdk 1.8,language level 1.8，添加 Hadoop 相关依赖：

```xml
        <dependency>
            <groupId>org.apache.hadoop</groupId>
            <artifactId>hadoop-common</artifactId>
            <version>2.10.2</version>
        </dependency>

        <dependency>
            <groupId>org.apache.hadoop</groupId>
            <artifactId>hadoop-hdfs</artifactId>
            <version>2.10.2</version>
        </dependency>
```
编写 HDFS 文件上传和读取程序。程序通过 Java API 连接到 Hadoop 集群，实现以下功能：

- 将本地文件上传到 HDFS；
- 从 HDFS 读取文件内容；
- 验证文件系统连接是否正常。

示例代码逻辑如下：
![1773752001408](image/大数据实验一/1773752001408.png)
## 4. 实验结果

通过实验 1.2，我成功完成了 Hadoop 集群的安装与配置，并能够正常使用 HDFS 进行文件读写操作。主要实验结果如下：

- 节点之间可以通过 SSH 免密登录；
![1773751831148](image/大数据实验一/1773751831148.png)
![1773752069802](image/大数据实验一/1773752069802.png)
从系统返回结果来看，执行 ssh node2 等命令后能够直接进入对应节点，说明公钥认证已经生效，authorized_keys 配置正确，节点间主机名映射也没有问题。
- 各节点 JDK 与 Hadoop 环境变量配置成功；
![1773751475819](image/大数据实验一/1773751475819.png)
Hadoop 是基于 Java 开发的分布式框架，因此 JDK 是其运行所必须依赖的软件环境。只有在系统正确安装 JDK 后，Hadoop 的各项服务进程才能启动。
从系统返回结果中可以看到 Java 版本号、运行环境以及虚拟机信息，这说明 Java 已经不仅安装完成，而且环境变量配置也已经生效。否则系统会提示“command not found”或者无法识别 Java 命令。
- Hadoop 集群启动正常；
- `jps` 能看到对应守护进程；
![1773751595995](image/大数据实验一/1773751595995.png)
![1773751621288](image/大数据实验一/1773751621288.png)
![1773751650366](image/大数据实验一/1773751650366.png)
![1773751663988](image/大数据实验一/1773751663988.png)
jps 命令用于查看当前节点上运行的 Java 进程，是检验 Hadoop 集群是否成功启动的重要方法。
从截图中如果能够看到 NameNode、DataNode、ResourceManager、NodeManager 等进程，就说明 HDFS 和 YARN 对应的核心服务已经成功启动。若缺少某个关键进程，则往往意味着配置文件错误、环境变量错误或节点通信异常。
这些进程名称并不是普通软件进程，而是 Hadoop 集群中承担不同职责的核心组件。例如 NameNode 负责管理文件系统元数据，DataNode 负责实际存储数据块，ResourceManager 和 NodeManager 则负责资源调度与任务管理。
因此，这张截图不仅是“启动成功”的证明，更体现了我已经理解 Hadoop 集群是由多个不同角色协同组成的分布式系统。
- Java 程序可以成功连接 HDFS 并完成文件上传、读取操作。
![1773752248805](image/大数据实验一/1773752248805.png)
程序首先检测当前 HDFS 文件系统状态，输出 HDFS is empty.，说明在执行上传操作前，HDFS 中尚未存在目标文件。随后程序依次执行文件上传、写入、下载和查看文件列表等操作，并在控制台中输出 Upload successfully!、Download successfully! 等提示信息，说明对应操作已经成功完成。
控制台中显示的 hdfs://1.92.86.251/... 路径说明程序访问的是远程分布式文件系统，而不是本地磁盘目录，这表明客户端与 NameNode 之间的通信已经建立成功。
从输出结果来看，程序完成了“上传文件—写入文件—下载文件—查看文件信息”的完整流程，说明 HDFS 的读写链路是连通的。其中，文件列表中显示了文件名称、是否为文件夹以及文件大小等属性，例如 folder: false 表示该对象是普通文件而不是目录，size: 64、size: 11 表示文件已经实际写入到 HDFS 中。
此外，控制台最上方出现的 Unable to load native-hadoop library for your platform... using builtin-java classes where applicable 是 Hadoop 在运行时常见的警告信息，它表示当前平台没有加载本地优化库，因此程序转而使用 Java 内置实现。这个提示不会影响当前实验功能的正常执行，因为后续上传、下载和查看文件都已经成功完成，说明系统整体运行正常。
- HDFS 文件系统状态正常，能够正确显示上传的文件。
![1773752261194](image/大数据实验一/1773752261194.png)
从实验过程来看，只有当前面上传成功、HDFS 正常存储该文件、程序正确定位目标路径并完成下载时，本地编辑器中才会显示出这一段完整文本。因此，这张截图实际上说明了 HDFS 的数据写入与读取过程都没有出现异常，文件内容在传输前后保持一致。
HDFS 不仅实现了文件“存在”，更重要的是实现了文件内容的正确保存与恢复。也就是说，分布式文件系统不仅要能记录文件名和路径，还必须能保证实际数据内容不丢失、不损坏。现在下载后的文件内容与原始内容一致，说明本次 HDFS 操作是成功且可靠的。

## 5. 实验分析

通过实验 1.2，我对 Hadoop 集群的搭建流程有了更加直观的认识。Hadoop 的部署不仅需要安装软件本身，更重要的是完成各节点之间的网络通信、主机名映射、环境变量配置及服务角色划分。免密登录是分布式集群正常协作的基础，而 JDK 与 Hadoop 的环境变量设置则是保证系统命令可执行的重要前提。

在 HDFS 实践部分，我掌握了通过 Java API 操作 HDFS 的基本方法，包括建立连接、文件上传与文件读取等。这说明 Hadoop 不仅可以通过命令行进行管理，也可以通过程序代码进行访问和控制，为后续 MapReduce 编程和大数据应用开发打下了基础。

此外，在实验过程中也体会到分布式系统部署对配置一致性的要求较高。任何一个节点环境变量错误、hosts 配置错误或 SSH 免密失败，都可能导致整个集群无法正常启动。因此在部署过程中必须逐项检查，确保所有节点配置一致。

## 6. 实验小结

实验 1.2 完成后，我已经初步具备了搭建 Hadoop 分布式环境和使用 HDFS 进行文件操作的能力。该实验为后续继续开展 MapReduce 实验和大数据处理任务提供了重要基础。

---

